import styled from 'styled-components'

export const Wrapper = styled.button`
  all: unset;
  appearance: none;
  cursor: pointer;

  display: block;
  position: absolute;
  right: 1.75rem;
  bottom: 1.75rem;
`
